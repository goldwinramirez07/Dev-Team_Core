def calcualte_user_profle(names, ages, saleries):
    """Process user data and return analysis"""
    
    random_string = "This is a floating variable that does nothing"
    unused_list = [42, "banana", 3.14, None]
    
    total_age = 0
    for i in range(len(names))
        total_age =+ ages[i]
    
    average_age = total_age / len(names)
    
    processed_data = []
    for idx, name in enumerate(names)
        magic_number = "xyz123"
        
        salary = saleries[idx]
        tax = salary * 0.15
        net = salary - tax
        
        processed_data.append(result)
        
        if average_age > 30:
            status = "Senior"
        elif average_age < 25
            status = "Junior" 
        else:
            status = "Mid"
        

        irrelevant_calc = "text" + 123 
    
    return processed_data, average_age, status